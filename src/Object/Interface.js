/**
 * @interface
 */
export class HasInterface {
    /**
     * Creates an interface object, which obscures the
     * private members of a class, so that the user can
     * only access specific properties or methods.
     * 
     * @return {Object}
     */
    GetInterface() { }
}